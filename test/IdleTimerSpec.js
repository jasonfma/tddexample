describe('IdleTimer', () => {
  describe('when idle time has passed', () => {
    describe('and there has been no activity', () => {
      it('should call the passed in idle action', () => {

      });
    });

    describe('and there has been activity', () => {
      it('should not call the passed in idle action', () => {

      });
    });
  });
});
